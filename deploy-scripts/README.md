# Deploy scripts

These scripts are sent to production server each time prod-deploy.yml action is run.

If app files location is changed, or nginx configuration location is changed, `lib.sh` script must be updated accordingly.
